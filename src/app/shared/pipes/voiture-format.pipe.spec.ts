import { VoitureFormatPipe } from './voiture-format.pipe';

describe('VoitureFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VoitureFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
