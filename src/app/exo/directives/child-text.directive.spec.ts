import { ChildTextDirective } from './child-text.directive';

describe('ChildTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildTextDirective();
    expect(directive).toBeTruthy();
  });
});
